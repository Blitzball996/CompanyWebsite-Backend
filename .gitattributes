# Auto detect text files and perform LF normalization
* text=auto

# Force LF for files executed/read on the Linux server (CRLF would break them)
*.sh        text eol=lf
Caddyfile   text eol=lf
*.yml       text eol=lf
*.yaml      text eol=lf
